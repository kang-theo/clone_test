class ApiServiceConstants {
  static const Duration connectTimeout = Duration(milliseconds: 6000);
  static const Duration receiveTimeout = Duration(milliseconds: 6000);
  static const String baseUrl = "http://localhost:8080";
}